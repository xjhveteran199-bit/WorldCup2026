<view class="wrap">
  <view class="card">
    <view class="card-h">🤖 AI 大模型连接</view>
    <view class="hint">填入任意一家国产大模型的 API Key 即可解锁 AI 战术分析。推荐 DeepSeek（注册即送额度）。Key 仅保存在你自己的手机本地，AI 内容由你的自有密钥生成。</view>

    <view class="row">
      <view class="label">渠道</view>
      <picker range="{{providers}}" value="{{provIdx}}" bindchange="onProv">
        <view class="sel">{{providers[provIdx]}} ▾</view>
      </picker>
    </view>

    <view class="row">
      <view class="label">模型</view>
      <picker range="{{models}}" value="{{modelIdx}}" bindchange="onModel">
        <view class="sel">{{models[modelIdx]}} ▾</view>
      </picker>
    </view>

    <view class="row">
      <view class="label">API Key <text class="apply" bindtap="copyUrl">复制申请地址 →</text></view>
      <input class="sel" password="{{true}}" placeholder="粘贴你的 API Key" value="{{key}}" bindinput="onKey"/>
      <view class="status {{statusType}}" wx:if="{{status}}">{{status}}</view>
    </view>

    <button class="btn" bindtap="save">验证并保存</button>
    <button class="btn btn-ghost" style="margin-top:16rpx" bindtap="clear">清除已存 Key</button>
  </view>

  <view class="disclaimer">
    <view>本工具为数据分析客户端，不提供 AI 服务；AI 内容由用户自有 API Key 调用第三方大模型生成。</view>
    <view>预测仅供娱乐参考，不构成任何竞彩建议。</view>
  </view>
</view>
